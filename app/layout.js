import './globals.css';

export const metadata = {
  title: 'XAU888 | Gold RSI Notification System',
  description: 'Real-time XAUUSD Gold Price RSI Monitor with Discord Notifications',
};

export default function RootLayout({ children }) {
  return (
    <html lang="th">
      <body>{children}</body>
    </html>
  );
}
